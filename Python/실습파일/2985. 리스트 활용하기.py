# zero_list 변수 선언
zero_list = [0]
print(zero_list)

# many_zero_list 변수 선언
many_zero_list = zero_list * 250000

# many_zero_list 길이 구하기
print(len(many_zero_list))

# numbers 변수 선언
numbers = list(range(1, 11))

# numbers,numbers 3번째부터 마지막 요소까지 출력
print(numbers)
print(numbers[3:])
