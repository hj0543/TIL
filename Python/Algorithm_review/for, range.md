
## 🔹 1. 기본 구조

```python
for i in range(5):
    print(i)
```

👉 출력:

```
0
1
2
3
4
```

📌 `range(5)`는 **0부터 4까지**의 숫자를 차례로 만들어 줌.

---

## 🔹 2. `range(start, stop)`

```python
for i in range(3, 8):
    print(i)
```

👉 출력:

```
3
4
5
6
7
```

📌 `range(3, 8)` → 3부터 시작해서 **8 직전까지** 반복.

---

## 🔹 3. `range(start, stop, step)` (증가폭 지정)

```python
for i in range(2, 11, 2):
    print(i)
```

👉 출력:

```
2
4
6
8
10
```

📌 `step=2` → 2씩 증가.

---

## 🔹 4. 감소하는 반복 (역순)

```python
for i in range(5, 0, -1):
    print(i)
```

👉 출력:

```
5
4
3
2
1
```

📌 `step`이 음수면 **감소** 가능.

---

## 🔹 5. 왜 `range`를 쓰는가?

* 메모리를 거의 사용하지 않음 (필요할 때 하나씩 생성)
* 반복 횟수가 명확할 때 가장 적합
* 인덱스 기반 반복에 매우 편리

---

## 🔹 6. 실제 예시들

### ✅ 별 찍기

```python
N = int(input())
for i in range(1, N + 1):
    print('*' * i)
```

### ✅ 합계 구하기

```python
total = 0
for i in range(1, 101):
    total += i
print(total)
```

### ✅ 리스트 인덱스 접근

```python
lst = ['a', 'b', 'c']
for i in range(len(lst)):
    print(i, lst[i])
```

---

## 🧠 핵심 요약

| 형태               | 의미               |
| ---------------- | ---------------- |
| `range(n)`       | 0부터 n-1까지        |
| `range(a, b)`    | a부터 b-1까지        |
| `range(a, b, c)` | a부터 b-1까지, c씩 증가 |

