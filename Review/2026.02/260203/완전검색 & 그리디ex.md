
# 📝 실전 문제로 보는 완전 검색 & 그리디

## 1. 그리디 알고리즘: 거스름돈 문제

탐욕 기법의 가장 대표적인 예시이다. "지금 당장 줄 수 있는 가장 큰 동전을 주는 것이 최적"이라는 전제가 성립하기 때문에 가능하다.

### ❓ 문제

손님에게 `1260원`을 거슬러 주어야 한다. 가지고 있는 동전은 `500원`, `100원`, `50원`, `10원`이다. 거슬러 줘야 할 동전의 **최소 개수**를 구하시오.

### 💡 풀이 접근

1. 가장 큰 단위의 동전(`500원`)부터 거슬러 줄 수 있는 만큼 최대한 준다.
2. 남은 돈을 다음 단위(`100원`)로 최대한 준다.
3. 이 과정을 끝까지 반복한다.

### 💻 코드

```python
def min_coin_count(money):
    # 큰 단위부터 정렬된 동전 리스트
    coins = [500, 100, 50, 10]
    count = 0
    
    for coin in coins:
        # 해당 동전으로 거슬러 줄 수 있는 개수 (몫)
        count += money // coin 
        # 남은 돈 갱신 (나머지)
        money %= coin 
        
    return count

target = 1260
result = min_coin_count(target)
print(f"필요한 동전의 최소 개수: {result}개") 
# 출력: 6개 (500*2, 100*2, 50*1, 10*1)

```

---

## 2. 배열 활용 문제: Gravity (낙하)

완전 검색이라기보다는 배열의 성질을 이용한 논리 문제이지만, 초반 알고리즘 수업(List 파트)에서 자주 다루는 예제이다.

### ❓ 문제

상자들이 쌓여있는 방이 있다. 방을 오른쪽으로 90도 회전시켰을 때, **상자가 가장 큰 낙차(떨어지는 높이)를 보이는 값**을 구하시오.

* 상자는 중력의 영향을 받아 바닥(오른쪽 끝)으로 떨어진다.
* 상자끼리 부딪히면 멈춘다.

### 💡 풀이 접근

물리 엔진처럼 실제로 이동시킬 필요가 없다.

1. 현재 내 위치보다 **오른쪽에 있는 상자의 개수**를 센다.
2. 전체 길이에서 `(내 현재 위치 + 오른쪽에 있는 상자 개수)`를 빼거나, 간단하게 **`전체 길이 - 1 - 현재 인덱스 - 내 오른쪽 상자 수`** 공식을 유도할 수 있다.
3. 즉, **내 밑(오른쪽)에 빈칸이 몇 개인지** 세는 것과 같다.

### 💻 코드

```python
def max_gravity_drop(boxes):
    '''
    boxes: 각 열에 쌓인 상자의 높이 리스트
    예: [7, 4, 2, 0, 0, 6, 0, 7, 0]
    '''
    max_drop = 0
    N = len(boxes)
    
    for i in range(N):
        # 현재 위치(i)의 상자 높이
        current_height = boxes[i]
        
        # 현재 위치보다 오른쪽에 있는 상자들 중,
        # 나(current_height)보다 높거나 같은 상자의 개수를 센다.
        # (내가 떨어지다가 걔네들 위에 쌓일 것이므로)
        blocks_below_me = 0
        for j in range(i + 1, N):
            if boxes[j] >= current_height: # 여기가 핵심! 나보다 크거나 같은 것만 장애물임
                blocks_below_me += 1
                
        # 낙차 계산: (전체 길이 - 1 - 현재 인덱스) - (내 밑에 깔릴 상자 수)
        # 더 직관적으로: 내 오른쪽에 있는 '빈 공간'의 개수만큼 떨어진다.
        # 빈 공간 수 = (오른쪽 전체 칸 수) - (오른쪽 장애물 수)
        drop = (N - 1 - i) - blocks_below_me
        
        if drop > max_drop:
            max_drop = drop
            
    return max_drop

data = [7, 4, 2, 0, 0, 6, 0, 7, 0]
print(f"최대 낙차: {max_gravity_drop(data)}")

```

---

## 3. 완전 검색 (순열): 3장의 카드 합

가장 기초적인 완전 검색 예제이다.

### ❓ 문제

주어진 숫자 카드 3장을 뽑아 나열했을 때, 그 합이 특정 값 `Target`이 되는 경우가 존재하는지 확인하시오. (단, 카드는 중복 사용 불가)

### 💻 코드

```python
def solve(cards, target):
    N = len(cards)
    # 3중 for문으로 모든 경우의 수 확인 (순열)
    for i in range(N):
        for j in range(N):
            if i == j: continue # 중복 카드 방지
            
            for k in range(N):
                if k == i or k == j: continue # 중복 카드 방지
                
                # 모든 경우를 다 만들어보고 조건 검사
                if cards[i] + cards[j] + cards[k] == target:
                    return True # 찾았다!
                    
    return False # 끝까지 없으면 실패

my_cards = [1, 5, 3, 8, 2]
print(solve(my_cards, 10)) # True (5+3+2)

```
