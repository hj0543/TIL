
# 📝 파이썬 딕셔너리 (Dictionary)

**Key-Value(키-값)** 쌍으로 데이터를 저장하는 가장 중요하고 강력한 자료구조이다. (별 다섯 개! ★★★★★)

## 1. 특징

* **해시 테이블(Hash Table)** 구조를 사용하여 데이터 크기와 상관없이 **검색 속도가 매우 빠르다.**
* **Key**: 고유해야 하며, 불변(Immutable) 객체만 가능하다. (중복 불가)
* **Value**: 중복이 가능하며, 어떤 자료형(List, Dict 등)이든 올 수 있다.

---

## 2. 메서드 요약 표

| 구분 | 메서드 | 설명 |
| --- | --- | --- |
| **조회** | **`D.get(k, [v])`** | Key로 Value 조회 (없으면 `None` 또는 `v` 반환) |
|  | **`D.keys()`** | 모든 **Key** 목록 반환 |
|  | **`D.values()`** | 모든 **Value** 목록 반환 |
|  | **`D.items()`** | 모든 **(Key, Value) 튜플** 목록 반환 |
| **수정/추가** | **`D.setdefault(k, v)`** | Key가 있으면 조회, 없으면 `(k, v)` 추가 후 반환 |
|  | **`D.update(other)`** | 다른 딕셔너리나 데이터로 병합 (덮어쓰기) |
| **삭제** | **`D.pop(k, [v])`** | Key 삭제 후 Value 반환 (없으면 Error 또는 `v`) |
|  | **`D.clear()`** | 모든 요소 삭제 (빈 딕셔너리 만듦) |

---

## 3. 조회 메서드 (Retrieval)

### ① `get(key[, default])` vs `[]` 접근

가장 안전하게 값을 가져오는 방법이다.

* **`D[key]`**: 키가 없으면 `KeyError`가 발생하여 프로그램이 멈춘다.
* **`D.get(key)`**: 키가 없으면 `None`을 반환하므로 안전하다.

```python
person = {'name': 'Alice'}

print(person.get('name'))           # Alice
print(person.get('age'))            # None (에러 안 남)
print(person.get('age', 'Unknown')) # Unknown (기본값 설정 가능)

```

### ② `keys()`, `values()`, `items()`

딕셔너리의 내부 요소를 꺼내볼 때 사용한다. 반복문과 함께 자주 쓰인다.

* **`items()`**는 Key와 Value를 튜플로 묶어주므로, **Unpacking(언패킹)** 하여 순회할 때 매우 유용하다.

```python
data = {'A': 10, 'B': 20}

# keys()
print(data.keys())   # dict_keys(['A', 'B'])

# values()
print(data.values()) # dict_values([10, 20])

# items() -> (Key, Value) 튜플 형태
for key, val in data.items():
    print(f"키: {key}, 값: {val}")

```

---

## 4. 추가 및 수정 메서드 (Update)

### ① `setdefault(key, default)`

**"조회 + 추가"** 를 동시에 수행한다.

* 키가 이미 있다면? -> 해당 값을 반환한다. (수정 X)
* 키가 없다면? -> `default` 값으로 데이터를 추가하고 반환한다.

```python
person = {'name': 'Alice'}

# 1. 이미 있는 키 조회
print(person.setdefault('name', 'Bob')) # Alice (안 바뀜!)

# 2. 없는 키 추가
print(person.setdefault('age', 25))     # 25 (추가됨)
print(person) # {'name': 'Alice', 'age': 25}

```

### ② `update(other)`

데이터를 **병합(Merge)** 하거나 **일괄 수정**할 때 사용한다.

* 기존 키가 있으면 값을 덮어쓰고(Overwrite), 없으면 새로 추가한다.

```python
person = {'name': 'Alice', 'age': 25}

# 딕셔너리로 병합
person.update({'age': 30, 'city': 'Seoul'}) 
# age는 30으로 수정, city는 추가됨

# 키워드 인자로 병합
person.update(country='Korea')

print(person) 
# {'name': 'Alice', 'age': 30, 'city': 'Seoul', 'country': 'Korea'}

```

---

## 5. 삭제 메서드 (Delete)

### ① `pop(key[, default])`

특정 데이터를 꺼내오면서 삭제한다. 리스트의 `pop()`과 비슷하지만, **Key를 지정**해야 한다.

* 키가 없을 때 `default`를 지정하지 않으면 에러가 발생하므로 주의해야 한다.

```python
data = {'a': 10, 'b': 20}

val = data.pop('a')
print(val)  # 10
print(data) # {'b': 20}

# 없는 키 삭제 시도 (default 지정)
print(data.pop('z', 0)) # 0 (에러 없이 기본값 반환)

```

### ② `clear()`

딕셔너리의 내용을 깨끗하게 비운다. (변수는 남고 내용물만 삭제)

```python
data.clear()
print(data) # {}

```

---

✅ **다음 단계**   
딕셔너리의 가장 강력한 활용법인 **"리스트에 있는 단어들의 등장 횟수 세기 (Counter)"** 로직을 `get()` 또는 `setdefault()`를 사용하여 구현해보자. (알고리즘 문제에서 매우 자주 나온다!)
