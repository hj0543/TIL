
# 📝 파이썬 세트 (Set)

**중복을 허용하지 않고, 순서가 없는** 고유한 항목들의 모음이다. (중요도 ★★)

## 1. 핵심 특징

* **고유성 (Unique)**: 중복된 데이터가 들어오면 자동으로 하나만 남긴다.
* **비순서성 (Unordered)**: 인덱스(Index)가 없어 `s[0]`처럼 접근할 수 없다.
* **해시 테이블 (Hash Table)**: 내부적으로 해시 구조를 사용하여 데이터의 크기와 상관없이 **검색/추가/삭제가 매우 빠르다.**

> **주의**: 빈 세트를 만들 때는 `{}`가 아니라 **`set()`**을 사용해야 한다. (`{}`는 빈 딕셔너리로 인식됨)

---

## 2. 추가 및 수정 메서드

데이터를 더하는 방식이다.

### ① `s.add(x)`

항목 하나를 추가한다. 이미 있는 값이면 변화가 없다.

```python
my_set = {1, 2, 3}
my_set.add(4)
my_set.add(4) # 중복 무시
print(my_set) # {1, 2, 3, 4}

```

### ② `s.update(iterable)`

여러 개의 값을 한 번에 추가한다. **리스트나 튜플 등을 넣으면 요소들이 풀려서 들어간다.**

```python
my_set = {1, 2}
my_set.update([2, 3, 4])
print(my_set) # {1, 2, 3, 4}

```

---

## 3. 삭제 메서드 비교 (핵심)

값을 지우는 방법이 다양하며, **에러 발생 여부**가 가장 큰 차이점이다.

| 메서드 | 설명 | 없는 값 삭제 시 |
| --- | --- | --- |
| **`s.remove(x)`** | 항목 `x`를 삭제 | **KeyError 발생** |
| **`s.discard(x)`** | 항목 `x`를 삭제 | **에러 없음 (조용히 넘어감)** |
| **`s.pop()`** | **임의의 요소**를 꺼내고 삭제 | 비어있으면 KeyError |
| **`s.clear()`** | 모든 항목 삭제 | - |

```python
s = {1, 2, 3}

# remove vs discard
s.remove(1)   # {2, 3}
# s.remove(99) -> KeyError 발생!

s.discard(2)  # {3}
s.discard(99) # 에러 없음, 그냥 통과

# pop (순서가 없으므로 무엇이 빠질지 확신할 수 없음)
val = s.pop() 
print(f"빠진 값: {val}, 남은 세트: {s}")

```

---

## 4. 집합 연산 (Set Operations)

세트는 수학의 집합과 동일한 연산을 지원한다. **연산자(기호)**와 **메서드** 두 가지 방법으로 사용할 수 있다.

### ① 합집합 (Union)

두 세트의 모든 항목을 합친다.

* 연산자: `|`
* 메서드: `union()`

```python
a = {1, 2}
b = {2, 3}
print(a | b) # {1, 2, 3}

```

### ② 교집합 (Intersection)

두 세트에 **공통으로 들어있는** 항목만 남긴다.

* 연산자: `&`
* 메서드: `intersection()`

```python
print(a & b) # {2}

```

### ③ 차집합 (Difference)

한 세트에서 다른 세트의 항목을 뺀다.

* 연산자: `-`
* 메서드: `difference()`

```python
print(a - b) # {1} (a에서 b에 있는 2가 빠짐)

```

### ④ 부분/상위 집합 확인

결과를 `True` / `False`로 반환한다.

* **`issubset` (`<=`)**: 부분집합인지 확인 (포함되어 있는가?)
* **`issuperset` (`>=`)**: 상위집합인지 확인 (포함하고 있는가?)

```python
set1 = {1, 2}
set2 = {1, 2, 3}

print(set1 <= set2) # True (set1이 set2 안에 포함됨)

```

---

✅ **다음 단계**
파이썬의 데이터 타입 중 **Mutable(리스트, 세트, 딕셔너리)**과 **Immutable(문자열, 튜플, 정수)**을 구분하는 것은 매우 중요하다.
**삭제 메서드 간의 차이**와 **집합 연산**은 코딩 테스트나 실무에서 데이터 필터링 시 자주 사용되므로 명확히 구분해야 한다.
이 개념과 연관지어, **"왜 리스트는 딕셔너리의 Key나 세트의 요소(Element)가 될 수 없는가?" (Hashable의 개념)**에 대해 정리해 보자.
