# 🐍 Python Math 모듈 핵심 정리

알고리즘 문제 풀이(PS)에서 자주 사용되는 `math` 내장 함수 요약입니다.
사용 전 반드시 `import math`가 필요합니다.

## 1. 정수론 및 조합론 (빈출)

| 함수 | 설명 | 예시 (`a=12, b=18`) | 비고 |
| :-- | :-- | :-- | :-- |
| `math.gcd(a, b)` | **최대공약수** (Greatest Common Divisor) | `math.gcd(12, 18)` → `6` | 유클리드 호제법 대체 |
| `math.lcm(a, b)` | **최소공배수** (Least Common Multiple) | `math.lcm(12, 18)` → `36` | **Python 3.9+** 부터 지원 |
| `math.factorial(x)` | **팩토리얼** ($x!$) | `math.factorial(5)` → `120` | $1 \times 2 \times \dots \times x$ |
| `math.comb(n, k)` | **조합** (${}_nC_k$) | `math.comb(5, 2)` → `10` | 순서 $X$, $n$개 중 $k$개 뽑기 |
| `math.perm(n, k)` | **순열** (${}_nP_k$) | `math.perm(5, 2)` → `20` | 순서 $O$, $n$개 중 $k$개 나열 |

## 2. 수치 계산 및 변환

| 함수 | 설명 | 예시 | 비고 |
| :-- | :-- | :-- | :-- |
| `math.sqrt(x)` | **제곱근** (결과: float) | `math.sqrt(16)` → `4.0` | 소수 판별 루프 범위 설정 시 사용 |
| `math.isqrt(n)` | **정수 제곱근** (결과: int) | `math.isqrt(10)` → `3` | $\lfloor\sqrt{n}\rfloor$과 동일, **대소비교 시 유용** |
| `math.pow(x, y)` | **거듭제곱** ($x^y$) | `math.pow(2, 10)` → `1024.0` | 정수 연산은 `**` 연산자가 더 빠르고 정확함 |
| `math.ceil(x)` | **올림** | `math.ceil(3.14)` → `4` | |
| `math.floor(x)` | **내림** | `math.floor(3.14)` → `3` | `int()`와 비슷하나 음수일 때 다름* |
| `math.trunc(x)` | **버림** (0을 향해 절삭) | `math.trunc(-3.14)` → `-3` | `int()` 변환과 동일한 효과 |

> *참고: `math.floor(-3.14)`는 `-4`, `int(-3.14)`는 `-3`이 됨.*

## 3. 상수 및 특수값

| 상수 | 설명 | 활용 예시 |
| :-- | :-- | :-- |
| `math.inf` | **양의 무한대** | 최솟값 찾기 초기화: `min_val = math.inf` |
| `-math.inf` | **음의 무한대** | 최댓값 찾기 초기화: `max_val = -math.inf` |
| `math.pi` | **원주율** ($\pi$) | `3.141592...` |

## 4. 활용 예제 코드

```python
import math

# 1. 최소공배수 (LCM) - Python 3.9 미만일 경우 공식 사용
def get_lcm(a, b):
    return (a * b) // math.gcd(a, b)

# 2. 소수 판별 범위 지정 시 isqrt 사용 (속도 및 타입 이점)
n = 100
for i in range(2, math.isqrt(n) + 1):
    if n % i == 0:
        print("소수 아님")
        break

# 3. 최솟값 구하기 초기화 패턴
min_value = math.inf
for num in [10, 5, 20]:
    if num < min_value:
        min_value = num