
## 3. 가로로 공백을 기준으로 나열된 정수 받기

한 줄에 `10 20 30 40 50` 처럼 공백으로 띄워진 숫자들이 들어올 때 사용한다.

### A. 리스트로 한 번에 담기 (가장 많이 씀)

입력 개수가 많거나, 개수가 정해지지 않았을 때 사용한다.

```python
import sys
input = sys.stdin.readline

# 입력 예시: 10 20 30 40 50
nums = list(map(int, input().split()))

print(nums) 
# 결과: [10, 20, 30, 40, 50]

```

**📌 동작 원리 3단계:**

1. **`.split()`**: 문자열을 공백 기준으로 쪼갬 (`['10', '20', ...]`)
    1-1.**`.split(,)`**: 문자열을 , 기준으로 쪼갬
2. **`map(int, ...)`**: 쪼개진 문자들을 하나하나 숫자로 변환 (`10, 20, ...`)
3. **`list(...)`**: 변환된 숫자들을 묶어서 리스트로 만듦 (`[10, 20, ...]`)

---

### B. 변수 각각에 나눠 담기 (개수가 고정될 때)

문제에서 "두 정수 A, B가 주어진다" 처럼 개수가 딱 정해져 있을 때는 리스트를 안 만드는 게 더 편하다.

```python
# 입력 예시: 3 7
a, b = map(int, input().split())

print(a) # 3
print(b) # 7

```

> **주의:** 입력된 숫자의 개수와 변수의 개수(`a, b`)가 정확히 일치해야 합니다. (다르면 에러 발생)

---

### 💡 요약 (TIL용)

| 상황 | 코드 패턴 |
| --- | --- |
| **N개의 숫자 → 리스트** | `data = list(map(int, input().split()))` |
| **고정된 숫자 → 변수** | `a, b, c = map(int, input().split())` |