
# 📝 Python 입력 패턴 모음 (상황별 정리)

기본 설정:

```python
import sys
input = sys.stdin.readline

```

## 1. 첫 줄에 N, 다음 N줄에 정수 1개씩 (세로)

데이터가 한 줄에 하나씩 내려오는 경우. **List Comprehension**을 쓰면 한 줄로 처리가 가능하다.

```python
# 입력:
# 5
# 1
# 10
# 4
# 9
# 2

N = int(input())
nums = [int(input()) for _ in range(N)]

# 결과: [1, 10, 4, 9, 2]

```

## 2. 테스트케이스 T, 이후 T줄 (매 줄마다 처리)

`A+B` 문제처럼 입력을 받자마자 바로 계산하고 출력해야 할 때 주로 사용한다.

```python
# 입력:
# 3
# 1 2
# 3 4
# 5 6

T = int(input())

for _ in range(T):
    a, b = map(int, input().split())
    print(a + b) # 로직 수행
    
# 결과:
# 3
# 7
# 11

```

## 3. N이 없는 EOF (입력 끝까지 받기)

입력이 몇 개인지 알려주지 않고, "입력이 끝날 때까지 받아라" 하는 경우. `sys.stdin.read()`가 가장 강력하다.

```python
# 입력:
# 7
# 3
# ... (끝을 모름)

# 방법 1: read()로 통째로 읽어서 나누기 (가장 추천 👍)
# 파일 전체를 읽어 공백/줄바꿈 기준으로 다 쪼개줌
import sys
nums = list(map(int, sys.stdin.read().split()))


# 방법 2: 줄 단위 처리가 꼭 필요할 때 (try-except)
while True:
    try:
        line = input()
        if not line: break # 빈 문자열이면 종료
        n = int(line)
        # 로직 수행
    except: # EOF 에러 발생 시 종료
        break

```

## 4. 특정 값(Sentinel)이 나오면 종료

"0 0 이 들어오면 종료한다" 혹은 "0이 들어오면 끝" 같은 조건이 있을 때. `while True`와 `break`를 쓴다.

```python
# 입력:
# 1 2
# 3 4
# 0 0  (종료 조건)

while True:
    a, b = map(int, input().split())
    
    if a == 0 and b == 0: # 종료 조건 확인
        break
        
    print(a + b)

```

---

### 💡 한눈에 보는 요약

| 상황 | 코드 패턴 | 비고 |
| --- | --- | --- |
| **가로 N개** | `list(map(int, input().split()))` | `split` 후 `map` |
| **세로 N줄** | `[int(input()) for _ in range(N)]` | 리스트 컴프리헨션 |
| **EOF (끝까지)** | `sys.stdin.read().split()` | 통째로 읽기 (강력) |
| **0이면 종료** | `while True:` → `if n==0: break` | 무한루프 + 탈출 |
