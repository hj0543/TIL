
# 📝 Python 2차원 배열(Grid/Graph) 입력 총정리
그래프(DFS/BFS), 미로 찾기, DP 문제에서 필수적으로 쓰이는 코드
기본 세팅:

```python
import sys
input = sys.stdin.readline

```

## 1. 공백이 있는 정수 배열 (가장 표준)

지도나 그래프 문제에서 숫자가 띄엄띄엄 주어질 때 사용한다.

```python
# 입력 (N=3, M=4):
# 1 2 3 4
# 5 6 7 8
# 9 0 1 2

N = int(input()) # 행 개수
# 리스트 컴프리헨션 사용 (가장 빠름)
grid = [list(map(int, input().split())) for _ in range(N)]

# 결과: [[1, 2, 3, 4], [5, 6, 7, 8], [9, 0, 1, 2]]

```

## 2. 공백 없는 붙어있는 숫자 (미로, 벽 부수기)

`101011` 처럼 숫자가 다닥다닥 붙어있을 때 사용한다. **`split()`을 쓰지 않는 것**이 핵심이다.

```python
# 입력 (N=3):
# 101
# 010
# 111

N = int(input())
# 문자열 한 글자씩 떼어서 숫자로 변환
# 주의: .rstrip() 필수! (뒤에 붙은 \n 제거 후 map 돌려야 함)
grid = [list(map(int, input().rstrip())) for _ in range(N)]

# 결과: [[1, 0, 1], [0, 1, 0], [1, 1, 1]]

```

## 3. 정수 변환 없이 문자로 유지할 때

특정 기호(`#`, `.`, `*`)로 된 맵을 입력받을 때 쓴다.

```python
# 입력:
# .#...
# ...#.

grid = [list(input().rstrip()) for _ in range(N)]

# 결과: [['.', '#', '.', '.', '.'], ...]
# Tip: 나중에 수정할 일이 없다면 list() 생략하고 문자열 리스트로 둬도 됨

```

## 4. 0으로 초기화된 빈 방(DP 테이블, 방문 배열) 만들기

입력을 받는 게 아니라, 계산을 위해 `0`으로 가득 찬 2차원 배열을 만들 때

```python
# N행 M열의 0 배열 생성
N, M = 5, 5

# ✅ [올바른 방법]
visited = [[0] * M for _ in range(N)]

# ❌ [절대 쓰면 안 되는 방법] (얕은 복사 버그 발생)
# visited = [[0] * M] * N  
# -> 이렇게 하면 첫째 줄 바꾸면 모든 줄이 같이 바뀜!

```

---

## ⚡ [심화] 입력 데이터가 엄청 클 때 (Iterator 활용)

입력이 수십만 줄일 때 `readline`을 `for`문 돌리는 것보다 `read().split()`으로 한 번에 가져와서 잘라 쓰는 게 훨씬 빠르다.

```python
import sys

# 1. 파일 전체를 읽어 공백 기준으로 토큰화
input_data = sys.stdin.read().split()

# 2. iterator 생성 (하나씩 꺼내 쓰기 위해)
iterator = iter(input_data)

N = int(next(iterator))
M = int(next(iterator))

# 3. N행 M열 데이터 채우기
# next(iterator)로 계속 다음 숫자를 꺼내옴
grid = [[int(next(iterator)) for _ in range(M)] for _ in range(N)]

```

---

### 💡 요약표

| 상황 | 코드 패턴 | 비고 |
| --- | --- | --- |
| **공백 O** (일반) | `[list(map(int, input().split())) for _ in range(N)]` | 국룰 (가장 많이 씀) |
| **공백 X** (미로) | `[list(map(int, input().rstrip())) for _ in range(N)]` | `rstrip` 필수 |
| **문자열** (기호) | `[list(input().rstrip()) for _ in range(N)]` | 정수 변환 생략 |
| **초기화** (0 채우기) | `[[0] * M for _ in range(N)]` | `* N` 실수 주의 |